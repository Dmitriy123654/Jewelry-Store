@model IEnumerable<Category>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-info">Category List</h2>
        </div>
        <div class="col-6 text-end">
            <a asp-controller="Category" asp-action="Create" class="btn btn-primary">
             <i class="fa-solid fa-plus"></i> &nbsp; Create New Category </a>
        </div>
    </div>

    <br /><br />
    @if(Model.Count() > 0)
    {
        <table class="table table-bordered table table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Category Name
                    </th>
                    <th>
                        Display Order
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach(var x in Model)
                {
                    <tr>
                        <td width="50%">@x.Name</td>
                        <td width="30%">@x.DisplayOrder</td>
                        <td class="text-center">
                            <div class="w-50% btn-group" role="group">
                                <a asp-controller="Category" asp-route-Id="@x.CategoryId"asp-action="Edit" class="btn btn-primary mx-2"> <i class="fa-regular fa-pen-to-square"></i> </a>
                                <a asp-controller="Category" asp-route-Id="@x.CategoryId" asp-action="Delete" class="btn btn-danger mx-2"> <i class="fa-regular fa-trash-can"></i> </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }     else
    {
        <p>Category not found.</p>
    }

</div>
